{
  "name": "webrtc-fingerprinting",
  "version": "1.0.0",
  "scripts": {
    "jest": "node ./download-browsers.js && jest --testTimeout 5000 --maxWorkers=1 src/content/",
    "stylelint": "stylelint 'src/**/*.css'"
  },
  "devDependencies": {
    "@puppeteer/browsers": "^2.2.0",
    "http-server": "^14.1.0",
    "jest": "^29.7.0",
    "selenium-webdriver": "^4.12.0"
  }
}
